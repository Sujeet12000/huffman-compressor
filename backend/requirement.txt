# Huffman Compressor API — Python dependencies
# Install:  pip install -r requirements.txt
# Upgrade:  pip install -r requirements.txt --upgrade

# ── Web framework ────────────────────────────────────────────────────────────
fastapi==0.136.3

# ── ASGI server ──────────────────────────────────────────────────────────────
# [standard] pulls in uvloop + httptools for better performance
# Run dev server:  uvicorn main:app --reload --port 8000
uvicorn[standard]==0.49.0

# ── File upload support ───────────────────────────────────────────────────────
# Required by FastAPI's UploadFile / Form parsing
python-multipart==0.0.32